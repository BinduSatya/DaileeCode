{
  "problem_id": "3376",
  "title": "Longest Common Suffix Queries",
  "difficulty": "Hard",
  "code": "class Solution {\npublic:\n    vector<int> stringIndices(vector<string>& wordsContainer, vector<string>& wordsQuery) {\n        vector<int> result;\n        for (const auto& query : wordsQuery) {\n            int maxLen = 0, minLen = INT_MAX, index = -1;\n            for (int i = 0; i < wordsContainer.size(); i++) {\n                int commonLen = 0;\n                for (int j = query.size() - 1, k = wordsContainer[i].size() - 1; j >= 0 && k >= 0; j--, k--) {\n                    if (query[j] != wordsContainer[i][k]) break;\n                    commonLen++;\n                }\n                if (commonLen > maxLen) {\n                    maxLen = commonLen;\n                    minLen = wordsContainer[i].size();\n                    index = i;\n                } else if (commonLen == maxLen) {\n                    if (wordsContainer[i].size() < minLen) {\n                        minLen = wordsContainer[i].size();\n                        index = i;\n                    }\n                }\n            }\n            result.push_back(index);\n        }\n        return result;\n    }\n};",
  "explanation": "* Intuition: The core insight here is that we need to find the string in `wordsContainer` that has the longest common suffix with each string in `wordsQuery`. We compare each string in `wordsQuery` with every string in `wordsContainer` to find the one with the longest common suffix. If there are multiple strings with the same longest common suffix, we choose the one with the smallest length.\n* Approach: For each string in `wordsQuery`, we iterate through all strings in `wordsContainer`. For each pair, we find the length of their common suffix by comparing characters from the end. We keep track of the string with the longest common suffix found so far and its length. If we find another string with the same longest common suffix but a smaller length, we update our result.\n* Time Complexity: O(n * m * k), where n is the number of strings in `wordsQuery`, m is the number of strings in `wordsContainer`, and k is the maximum length of a string. This is because we are iterating through each string in `wordsQuery` and comparing it with every string in `wordsContainer`, and for each comparison, we are iterating through the characters of the strings.\n* Space Complexity: O(n), where n is the number of strings in `wordsQuery`. This is because we are storing the result for each string in `wordsQuery` in the `result` vector.",
  "test_passed": true,
  "test_output": "Solution instantiated successfully\nSMOKE_TEST_PASSED\n"
}